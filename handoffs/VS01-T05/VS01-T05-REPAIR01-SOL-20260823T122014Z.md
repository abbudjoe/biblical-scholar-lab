# VS01-T05 Repair01 Sol Handoff

## Disposition

`READY_FOR_CHATGPT_REVIEW`

Draft PR: <https://github.com/abbudjoe/biblical-scholar-lab/pull/14>

- Starting exact head: `836543e325983b74e2da8773e69df78d010fcff8`
- Repair implementation head: `4033581da30d349d0aa0211d41bd864f607556d2`
- Repair implementation tree: `2ef0d213e82aa87337c59f4a3402252e9ddfe480`
- Base: `7db0ff4a1ade89c043a9330b89de90467dba612c`
- Cumulative handwritten active implementation: `1,800` nonblank substantive lines
- Final handoff-only head: recorded in the PR completion comment and final report after this self-referential commit exists

The repair preserves `ACT-VS01-T05-DETERMINISTIC-STUDY-RUNTIME-v1`, every frozen authority artifact, all four public contracts and generated schemas, the migration, PostgreSQL and dependency pins, both workflows, and all exact scholarly semantics and answer bytes. Only the four owner-permitted production/test paths and this append-only handoff pair changed.

## Finding closure

### T04 reload before persistence

Every live execution now loads and verifies T04 authority, builds and verifies deterministic artifacts, reloads and verifies T04 authority, compares one narrow semantic authority fingerprint, and only then reaches persistence. The comparison binds the resolved root, complete canonical packet semantic bytes, complete receipt semantic bytes, and receipt-file SHA; those semantic bytes include the required packet identity/SHA and receipt UUID, disposition, implementation commit, archive root, and publication paths.

A focused changed-second-load test proves the stable `T04 authority changed before persistence` failure, `persist_runtime` calls `0`, connection attempts `0`, and no retry. Dry run still loads authority once and performs no database access.

### Exact PostgreSQL schema gate

The former name-only check was replaced in place. PostgreSQL 18.6 catalog fingerprint `9c193d6cbe6b853b7edac7b605c3194b4a8ac7cc46363c1644b1a697e878fc2f` binds:

- exactly the three relations and no RLS;
- ordered names, PostgreSQL types, and nullability for every column;
- primary, unique, foreign-key, and CHECK constraints and their attributes;
- exact index set, access methods, keys, expressions/predicates, and canonical definitions;
- exact per-table trigger type and target function;
- the one `plpgsql` trigger function, trigger return type, and mutation-rejection source.

Pinned CI rejected an UPDATE-only recreated trigger, a removed unique, a removed CHECK, an extra view, and an extra trigger before any runtime insertion. The migration remained byte-preserved.

### Complete existing-run verification

`VERIFIED_EXISTING` now requires a complete reload and cross-check of all `study_run` root fields, all five artifact rows, the stored `PERSISTED` audit, and all eleven event rows. It validates UUIDv7/offset time, exact run key and packet/receipt/spec/executor bindings, artifact hashes/contracts/JSON/time/run ownership, audit run/session/implementation/count bindings, event relational columns against the JSON envelope, same-run artifact ownership, state/order, canonical event hashes, and the previous-hash chain.

Pinned PostgreSQL tests reject changed artifact content, wrong contract name, event artifact-column rebinding, event run rebinding, audit run/session rebinding, wrong stored authority, and wrong implementation commit. Exact replay writes nothing only after all checks pass.

### Correction scope and redacted provider failures

Request-level revision-1 lineage and the exact correction identity remain present in the frozen revision-2 request, execution record, answers, and audit. T05 persistence now creates only the active revision-2 canonical run: `supersedes_run_id = null`, one complete `1/5/11` transaction, and no fabricated revision-1 execution. Cross-run correction persistence is deferred.

The incomplete partial revision-1 seed and its private `_session_id`/`_supersedes_run_id` controls were removed. Connection and transaction `psycopg.Error` failures become `ValueError("database operation failed") from None`; project-owned schema/authority `ValueError` failures are not relabeled. The CLI returns exactly the machine-readable `OPERATION_FAILED` envelope without the fake secret, DSN, SQL, provider text, or traceback. A real ephemeral transaction failure rolls back to `0/0/0`.

## Validation

- `uv sync --frozen`: pass
- `uv lock --check`: pass
- `uv run ruff format --check .`: pass, 109 files
- `uv run ruff check .`: pass
- `uv run pyright`: pass, 0 errors and 0 warnings
- Full branch-aware suite: 403 passed, 33 skipped, 90.04% coverage
- Local skips: 24 PostgreSQL tests because `BSL_TEST_DATABASE_URL` was absent; 9 optional external-jsonschema tests
- `git diff --check 7db0ff4a1ade89c043a9330b89de90467dba612c...4033581da30d349d0aa0211d41bd864f607556d2`: pass
- `git fsck --full`: pass, dangling objects only
- Implementation-head `vs01-t01-ci`: pass — <https://github.com/abbudjoe/biblical-scholar-lab/actions/runs/32638923202>
- Implementation-head `vs01-t05-ci`: pass — <https://github.com/abbudjoe/biblical-scholar-lab/actions/runs/32638923201>
- PostgreSQL suite: 24 passed, zero skipped, server version `180006`
- Migration/schema conformance and exact head/diff steps: pass

The PostgreSQL suite proves exact schema/catalog rejection adversaries, all three UPDATE/DELETE mutation triggers, exact atomic `1/5/11` counts, rollback to zero, exact replay, changed-existing rejection, all relational/JSON cross-binding adversaries, revision-2-only correction scope, contiguous sequence, and the previous-event hash chain.

## Repaired canonical-archive dry runs

Both exact commands exited `0`, validated in JSON mode through all four frozen Pydantic contracts, and returned `DRY_RUN_VALIDATED`, `persisted=false`, and `verified_existing=false`.

Deterministic identities and hashes:

- Request identity: `5b0d413278c3dcf03421989dcec84228a828d13236a232977b448b14bd0b68ef`
- Request artifact SHA-256: `492ab7a840fa00a60861b6dc3b11abed82053b18503bccff3db97b3a73c15480`
- Execution identity: `f0697ce01cfa4579223a59042e49de7377d3ec5dee7cca29468bbab3cfba20cc`
- Execution artifact SHA-256: `a6aa9f78702b5fe57a95a3acc2537023a8bcbce4d336c96b7c6aa6c9c6ff91d3`
- Brief identity: `b97e895bfc640d0020f7146cd88112fd0f6f9ec8669bfab316c8d49688aef2a0`
- Brief artifact SHA-256: `8c279286f13e83a985654e41750c6b5e415dbee0561235d2172a275cb142c9f3`
- Brief Markdown SHA-256: `af13455c89f819ac999121b85ad611ef3e5ec9720f08a44856af00638bd3f2c0`
- Study identity: `f18264255ab9117cdd7fa40d9da35c614d31a9bfcabce7a5e95e7b0a040424fd`
- Study artifact SHA-256: `4dc1fec79b85d88c17121de3e66cca84e5eaa44c73f302289918ec4a7fc1b453`
- Study Markdown SHA-256: `aa32440433e71db2c74c24b2b43ec6f5ba077a15b5231517f5750eafb27b0cc6`

The request, execution record, both answer artifacts, and both Markdown strings were byte-identical across runs.

Audit 01:

- Receipt UUID: `01a02e8f-0d48-7a1f-a85b-1e2db13a0ee5`
- Receipt SHA-256: `15aa76f7f60b60c30edaf120b1b4da7b44303970b0d725f35616dbf57129824c`
- Captured file SHA-256: `60d07873da32df0ffa2030307295612d4294672d0493c51f9ca52f78e2fd2e3b`

Audit 02:

- Receipt UUID: `01a02e8f-1d0f-79c5-8d2e-eb2597288a09`
- Receipt SHA-256: `8b47b2a2064907daad0401f4e1b510bd2e9de968bcbb68d568d3efb75efa43a4`
- Captured file SHA-256: `d3318431250eca48b9a1e67154b6ffe7cc4fc4742e82c61e9ff8b735110d3511`

The actual audit differences were only `receipt_identity`, `generated_at`, and the derived `receipt_canonical_sha256`; measured latency was equal at 19 ms. Every deterministic binding, count, disposition, terminal state, and cost agreed.

The pre/post T04 authority evidence files were byte-identical with SHA-256 `6e8573d123fd2423f87688327d8c9e94a5d3d7fa835d5f73da4903abe99f3fec`. The packet remained `aebcbb50fc8383f2f4f395bc71116563325c1fde237427dc8c1bc8140e8ebe31` / `9f81621785924161cc4861e2af9f010bd18e822b60199d62a6327eff44ea0409`, and receipt `01a02bbe-bda5-776b-a95e-16bb40d18597` / `02272e1ec458a33e449aa93f9508a57d4eaacf3d9dccc48888f3952bbe96dad4` remained `PUBLISHED` at implementation commit `7db0ff4a1ade89c043a9330b89de90467dba612c`.

Across both runs and the pre/post measurement: canonical archive writes `0`, owner database connections/writes `0/0`, forbidden T03/raw-source reads `0`, network requests `0`, model invocations `0`, and canonical live T05 executions `0`.

## DR-30 receipt

Repair01 replaced 256 added/133 removed production lines and 184 added/247 removed test lines. Cumulative handwritten active implementation is exactly 1,800 nonblank substantive changed lines from the PR base. No generated, contract, schema, migration, dependency, workflow, public-contract, table, or CLI change occurred.

- Function/method logical lines: within 60
- Cyclomatic complexity: within 10; Ruff clean
- Nesting: within 3
- Production class: within 250 logical lines
- Largest production module: 441 physical lines, within 500
- Handwritten production files: 5 cumulative, within 7
- Public contracts/direct dependencies/migrations/workflows/schemas/tables: unchanged at `4/1/1/1/1/3`
- Waivers: none
- Simplicity conformance: `PASS`

No merge was performed. No approval was submitted. PR #14 remains draft. The published T04 packet and receipt were read and verified without mutation. No T03 normalization artifact, raw source snapshot, source manifest, quarantine record, admission record, or raw source object was read. No owner database was created, connected, migrated, or written, and no canonical live T05 run was executed. No model, application network, source, benchmark, page, retrieval, cloud, Lambda, training, evaluation, or billable operation occurred. No VS01-T06 or later task was started.

The next action belongs to ChatGPT exact-head review and Joseph's decision.
